<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto">

	<group android:checkableBehavior="single">
		<item
			android:id="@+id/action_sort_recent"
			android:title="@string/thread_sort_recent"
			app:showAsAction="never" />
		<item
			android:id="@+id/action_sort_newest"
			android:title="@string/thread_sort_newest"
			app:showAsAction="never" />
		<item
			android:id="@+id/action_sort_replies"
			android:title="@string/thread_sort_replies"
			app:showAsAction="never" />
	</group>

</menu>
